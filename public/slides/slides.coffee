// The methods here defined are called upon activation of a
// step with "event"="customEvent" attribute
// (if you just want a js step without an element appearing, use a hidden one)

customEvent = ->
  console.log 'custom event!'
